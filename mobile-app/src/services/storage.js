// Placeholder for Local Storage (e.g. AsyncStorage)
// Used for history, user settings, etc.

export const saveItem = async (key, value) => {
  console.log(`Saving ${key}:`, value);
};

export const getItem = async (key) => {
  return null;
};
